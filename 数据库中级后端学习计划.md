# 数据库中级后端学习计划

> 前端工程师转后端，MySQL 为主线，3-6 个月业余学习
> 目标：能独立设计数据库、优化查询、处理并发、了解数据库生态

---

## 学习原则

1. **每节课必须有代码/查询产出**，只看不写等于没学
2. **每个阶段结束做一个可展示的东西**（设计文档、优化报告、DEMO）
3. **遇到问题先查官方文档**，再查博客
4. **善用 Docker** 搭建环境，不用污染本机

---

## 环境准备（第 0 周）

```bash
# 安装 MySQL 8.x（任选一种）
# 方案 A：Docker（推荐）
docker run --name mysql8 -p 3306:3306 -e MYSQL_ROOT_PASSWORD=root -d mysql:8.0

# 方案 B：Homebrew（macOS）
brew install mysql@8.0

# 准备测试数据库
docker exec -it mysql8 mysql -uroot -proot
```

需要安装的工具：
- **MySQL客户端**：任意 GUI（Navicat / Sequel Ace / DBeaver）或命令行
- **编程语言驱动**：看你用 Node（mysql2）/ Python（pymysql）/ Go（database/sql）
- **dbdiagram.io 账号**：画 ER 图
- **Docker Desktop**：后面复制、主从需要

---

## 第一阶段：SQL 查漏补缺（第 1-2 周）

### 目标
掌握复杂查询，不只是 SELECT * FROM table

### 第 1 周：基础强化

#### Day 1-2：聚合与分组
```
学习内容：
  - COUNT, SUM, AVG, MAX, MIN
  - GROUP BY + 多字段分组
  - HAVING vs WHERE 的区别

练习（用人人都懂的员工部门表）：
-- 建表
CREATE TABLE emp (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50),
  dept_id INT,
  salary DECIMAL(10,2),
  hire_date DATE
);

-- 写出以下查询：
-- 1. 每个部门的平均工资
-- 2. 平均工资高于 5000 的部门
-- 3. 每个部门入职最早和最晚的员工日期
-- 4. 统计每个部门 salary > 3000 的人数
```

#### Day 3-4：多表连接
```
学习内容：
  - INNER JOIN, LEFT JOIN, RIGHT JOIN
  - 多表关联（3 表以上）
  - 自连接（同一张表自己 JOIN 自己）

练习（在 emp 表基础上加部门表）：
CREATE TABLE dept (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50)
);

-- 1. 查每个员工姓名 + 部门名
-- 2. 查没有员工的部门（LEFT JOIN + IS NULL）
-- 3. 查工资高于同部门平均工资的员工
-- 4. 自连接：查员工及其经理（假设 emp 有 manager_id 字段）
```

#### Day 5：子查询
```
学习内容：
  - WHERE 子查询（IN, EXISTS, ANY, ALL）
  - FROM 子查询（派生表）
  - SELECT 子查询（标量子查询）
  - EXISTS vs IN 性能差异

练习：
-- 1. 查工资高于公司平均的员工（标量子查询）
-- 2. 查有订单的客户（EXISTS vs IN 对比）
-- 3. 查每个部门工资最高的员工（FROM 子查询）
```

#### Day 6-7：CASE WHEN + UNION + 综合练习
```
学习内容：
  - CASE WHEN 做条件判断
  - UNION / UNION ALL 合并结果
  - 综合练习：用过去 7 天学的所有语法

综合练习场景：设计一个简单的订单查询报表
-- 按月份统计：总订单数、总收入、取消订单数
-- 用 CASE WHEN 标记订单金额区间（低/中/高）
-- 用 UNION 合并本年与去年数据
```

### 第 2 周：窗口函数与 CTE（中级 SQL 的敲门砖）

#### Day 1-2：窗口函数
```
学习内容：
  - ROW_NUMBER(), RANK(), DENSE_RANK()
  - LAG(), LEAD()
  - SUM() OVER(PARTITION BY ... ORDER BY ...)

练习：
-- 1. 查每个部门工资排名
-- 2. 查每个员工与上个月销售额的环比（LAG）
-- 3. 查每个分类下销量前三的商品
```

#### Day 3-4：CTE (Common Table Expressions)
```
学习内容：
  - WITH ... AS 基本用法
  - 递归 CTE（生成序列、树形结构）

练习：
-- 1. 用 CTE 简化复杂子查询
-- 2. 递归查组织架构树（员工-经理层级）
-- 3. 生成一个日期序列做报表补零
```

#### Day 5-7：复习 + 实战
```
实战项目：用你熟悉的语言（Node/Python/Go）写一个命令行工具
  连接 MySQL，执行查询，输出结果到控制台或 CSV 文件

要求：
  - 支持传入 SQL 文件路径执行
  - 支持输出格式选择（表格 / CSV）
  - 异常处理和超时控制
```

**里程碑产出：** 一个 SQL 查询工具箱（命令行工具）

---

## 第二阶段：MySQL 实战基础（第 3-4 周）

### 第 3 周：表设计与数据类型

#### Day 1-2：数据类型选型
```
学习内容：
  - 整型：TINYINT, INT, BIGINT 选型
  - 浮点：DECIMAL vs FLOAT/DOUBLE（钱用 DECIMAL）
  - 字符串：CHAR vs VARCHAR, TEXT 系列
  - 日期：DATETIME vs TIMESTAMP vs DATE
  - JSON 类型（MySQL 5.7+）

练习：
-- 设计用户表，思考每个字段用什么类型
-- 建表后插入数据测试边界
```

#### Day 3-4：约束与默认值
```
学习内容：
  - PRIMARY KEY（聚簇索引原理简介）
  - FOREIGN KEY（级联删除/更新）
  - UNIQUE, NOT NULL, DEFAULT, CHECK（MySQL 8.0+ 支持 CHECK）
  - AUTO_INCREMENT 注意事项

练习：
-- 给用户表加上各种约束
-- 测试外键级联行为（删用户 → 删关联数据）
-- 试试 CHECK 约束检查邮箱格式
```

#### Day 5-7：实战建表 - 博客系统
```
动手设计完整的博客系统数据库：

  users（用户表）
  posts（文章表）
  comments（评论表）
  tags（标签表）
  post_tags（文章-标签关联表）
  categories（分类表）

要求：
  - 先画 ER 图（用 dbdiagram.io）
  - 再写 DDL 建表
  - 插入测试数据（至少 5 篇文、20 条评论）
  - 写出常用查询（首页文章列表、文章详情含评论数、标签文章列表）
```

### 第 4 周：后端连数据库

#### Day 1-3：语言驱动操作 MySQL
```
按你熟悉的后端语言选择：

Node.js:
  - 用 mysql2 连接池
  - prepared statement 防注入
  - promise wrapper 使用

Python:
  - 用 pymysql 或 mysql-connector-python
  - 上下文管理器管理连接
  - 参数化查询

Go:
  - database/sql + go-sql-driver/mysql
  - sql.Rows 遍历
  - 预编译语句

练习：
  - 写一个 DB 工具类/模块，封装连接池
  - 实现增删改查基本操作
  - 测试 SQL 注入（看参数化查询如何防注入）
```

#### Day 4-7：REST API + MySQL
```
把博客数据库做成 REST API：

  GET    /posts          → 文章列表
  GET    /posts/:id      → 文章详情（含评论）
  POST   /posts          → 创建文章
  POST   /comments       → 发表评论
  GET    /tags/:id/posts → 标签下的文章

要求：
  - 使用连接池
  - 参数校验 + 错误处理
  - 返回 JSON 格式
```

**里程碑产出：** 博客系统 REST API，数据库 + 后端代码都完成

---

## 第三阶段：数据库设计（第 5-6 周）

### 第 5 周：ER 建模与范式

#### Day 1-2：ER 图
```
学习内容：
  - 实体、属性、关系（1:1, 1:N, M:N）
  - 如何从需求描述中识别实体
  - 用 dbdiagram.io 画 ER 图

练习：给定需求描述，画出 ER 图
  "一个班级有多个学生，一个学生可选多门课程，
   每门课程有多个学生，课程有考试成绩"
```

#### Day 3-4：三大范式
```
学习内容：
  - 1NF：字段不可再分
  - 2NF：非主属性完全依赖主键
  - 3NF：没有传递依赖
  - 什么时候可以反范式（查询性能 > 存储冗余）

练习：
  给一个违反范式的表，逐步拆解到 3NF：
  订单表（order_id, customer_name, customer_phone, product_name, product_price, quantity）
```

#### Day 5-7：常用设计模式
```
学习内容：
  - 树形结构（邻接表 vs 闭包表 vs 嵌套集）
  - 标签系统（多对多 vs 位运算）
  - 软删除（is_deleted 字段 vs 删除表）
  - 版本号/时间戳乐观锁
  - 枚举字段设计（TINYINT vs VARCHAR vs ENUM 类型）

练习：
  对比邻接表和闭包表，各插入 1 万条数据测试查询性能
```

### 第 6 周：实战设计 - 电商下单系统

```
需求描述：
  用户可以在平台浏览商品，加入购物车，下单购买，
  一个订单包含多个商品，支付后更新库存。

需要设计的表（至少）：
  - users（用户）
  - products（商品）
  - product_stock（库存）
  - carts（购物车）
  - cart_items（购物车项）
  - orders（订单）
  - order_items（订单项）
  - payments（支付记录）

要求：
  1. 画 ER 图
  2. 写出 DDL（含索引、约束、默认值）
  3. 写出完整的下单事务（扣库存 + 创建订单 + 清购物车）
  4. 考虑并发场景（多人同时买同一商品）

核心难点 - 库存扣减：
  高并发下怎么防止超卖？
  - 方案 A：事务 + SELECT FOR UPDATE
  - 方案 B：乐观锁（version 字段）
  - 方案 C：Redis 原子操作扣库存
  三种方案都实现并对比
```

**里程碑产出：** 电商数据库完整设计文档 + DDL + 下单事务 SQL

---

## 第四阶段：索引与性能优化（第 7-8 周）

### 第 7 周：索引原理

#### Day 1-2：B+ Tree 索引
```
学习内容：
  - B+ Tree 结构示意图
  - 聚簇索引（InnoDB 主键索引）
  - 二级索引与回表
  - 索引页分裂

不需要死磕源码，但要理解：
  - 为什么 B+ Tree 适合磁盘 IO
  - 为什么主键推荐自增 INT（避免页分裂）
  - 什么是索引高度（3-4 层就能存上亿数据）

练习：
  建一个无主键表和自增主键表，各插入 100 万行，观察插入性能差异
```

#### Day 3-4：索引类型与使用
```
学习内容：
  - 主键索引、唯一索引、普通索引、全文索引
  - 联合索引与最左前缀原则
  - 覆盖索引（Extra: Using index）
  - 索引下推（ICP, Index Condition Pushdown）

练习：
  在博客系统的 posts 表上加各种索引：
  - 联合索引 (category_id, created_at)
  - 测试哪些查询能用上这个索引
  - 哪些查询用不上（违反最左前缀）
```

#### Day 5-7：EXPLAIN 解读
```
学习内容：
  - type 字段：system > const > eq_ref > ref > range > index > ALL
  - key：实际使用哪个索引
  - rows：扫描行数估算
  - Extra：Using index, Using where, Using filesort, Using temporary

练习：
  -- 1. 找一条 type=ALL 的查询
  -- 2. 加索引后改成 type=ref 或 range
  -- 3. 观察 Using filesort 如何消除（ORDER BY 走索引）
  -- 4. 观察 Using temporary 如何消除
```

### 第 8 周：慢查询优化实战

#### Day 1-3：慢查询分析
```
学习内容：
  - 开启慢查询日志
  - pt-query-digest 或 mysqldumpslow 分析
  - performance_schema 查看等待事件

配置：
SET GLOBAL slow_query_log = ON;
SET GLOBAL long_query_time = 0.5;  -- 超过 0.5 秒记录
SET GLOBAL log_queries_not_using_indexes = ON;

练习：
  用存储过程往博客表灌 10 万行数据：
  ```sql
  -- 批量插入数据的存储过程
  DELIMITER $$
  CREATE PROCEDURE insert_mock_posts()
  BEGIN
    DECLARE i INT DEFAULT 1;
    WHILE i <= 100000 DO
      INSERT INTO posts(title, content, category_id, user_id, created_at)
      VALUES (CONCAT('Post ', i), REPEAT('content ', 20),
              FLOOR(1 + RAND()*10), FLOOR(1 + RAND()*100),
              DATE_SUB(NOW(), INTERVAL FLOOR(RAND()*365) DAY));
      SET i = i + 1;
    END WHILE;
  END$$
  DELIMITER ;
  CALL insert_mock_posts();
  ```

  然后：
  - 找 3 个慢查询
  - 每个分析并优化
  - 记录优化前后执行时间
```

#### Day 4-7：优化案例集
```
实战优化 5 个典型场景：

场景 1：分页偏移大
  SELECT * FROM posts ORDER BY created_at DESC LIMIT 100000, 20;
  → 优化：游标分页（WHERE created_at < ?）

场景 2：ORDER BY 不走索引
  → 优化：加合适的联合索引

场景 3：COUNT(*) 慢
  → 优化：使用二级索引计数

场景 4：OR 条件
  → 优化：改成 UNION

场景 5：JOIN 大表
  → 检查连接字段是否有索引、驱动表选择

产出物：一份查询优化报告，包含：
  优化前 SQL、执行时间、EXPLAIN 结果
  优化方案、优化后 SQL、执行时间
```

**里程碑产出：** 慢查询优化报告，至少 5 个案例

---

## 第五阶段：事务与锁（第 9-10 周）

### 第 9 周：事务基础

#### Day 1-2：ACID 与事务使用
```
学习内容：
  - BEGIN / COMMIT / ROLLBACK
  - 自动提交模式
  - 保存点（SAVEPOINT）

练习：
  BEGIN;
  UPDATE accounts SET balance = balance - 100 WHERE id = 1;
  UPDATE accounts SET balance = balance + 100 WHERE id = 2;
  COMMIT;

  -- 中间出错了，ROLLBACK 回滚
  -- 验证余额不变
```

#### Day 3-5：隔离级别
```
学习内容：
  - READ UNCOMMITTED → 脏读
  - READ COMMITTED → 不可重复读
  - REPEATABLE READ（MySQL 默认）→ 幻读
  - SERIALIZABLE → 性能最差

练习（开两个终端实测）：
  -- 终端 1
  SET SESSION TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;
  BEGIN;
  SELECT * FROM accounts;  -- 看到未提交数据（脏读）

  -- 终端 2
  BEGIN;
  UPDATE accounts SET balance = 200 WHERE id = 1;
  -- 先不 COMMIT，让终端 1 看到脏数据

  每个隔离级别都这样测一遍，记录观察到的现象
```

#### Day 6-7：MVCC 原理
```
学习内容：
  - UNDO LOG 版本链
  - ReadView 机制（活跃事务列表）
  - 可见性判断规则

不要求写代码，但要能用自己的话解释清楚：
  "为什么 REPEATABLE READ 下，同一个事务里多次读取结果一致？"
  → 因为事务首次读时生成了 ReadView，后续复用
```

### 第 10 周：锁与死锁

#### Day 1-3：InnoDB 锁类型
```
学习内容：
  - 行锁（Record Lock）：锁住索引记录
  - 间隙锁（Gap Lock）：锁住范围，防止幻读
  - 临键锁（Next-Key Lock）：行锁 + 间隙锁
  - 意向锁（Intention Lock）：表级别的"有人要锁行"标记
  - 表锁（LOCK TABLES）：什么时候需要用

练习：
  -- 间隙锁演示
  -- session A
  BEGIN;
  SELECT * FROM products WHERE price BETWEEN 100 AND 200 FOR UPDATE;

  -- session B
  INSERT INTO products(price) VALUES(150); -- 被间隙锁阻塞！
```

#### Day 4-6：死锁分析
```
学习内容：
  - 死锁产生的四个必要条件
  - 如何避免死锁（固定顺序、降低隔离级别、缩短事务）
  - 死锁排查命令

命令：
  SHOW ENGINE INNODB STATUS\G

故意制造死锁：

  -- session A
  BEGIN;
  UPDATE accounts SET balance = balance - 100 WHERE id = 1;
  -- 别提交
  UPDATE accounts SET balance = balance + 100 WHERE id = 2;

  -- session B（同时）
  BEGIN;
  UPDATE accounts SET balance = balance - 100 WHERE id = 2;
  -- 别提交
  UPDATE accounts SET balance = balance + 100 WHERE id = 1; -- 死锁！

解析 SHOW ENGINE INNODB STATUS 输出：
  - LATEST DETECTED DEADLOCK 部分
  - 找出哪个事务回滚了

练习：写完分析报告
```

#### Day 7：事务最佳实践
```
总结：
  - 事务尽可能短
  - 不要在事务中做远程调用（HTTP 请求）
  - 一致性要求不高的场景可降低隔离级别
  - 监控锁等待超时（innodb_lock_wait_timeout）
```

**里程碑产出：** 死锁分析与优化报告

---

## 第六阶段：高可用与运维基础（第 11-12 周）

### 第 11 周：备份与恢复

#### Day 1-3：逻辑备份
```
学习内容：
  - mysqldump 使用（全量备份）
  - --single-transaction 原理（利用 MVCC 做一致性备份）
  - 备份指定库/表
  - 压缩备份文件

命令：
  mysqldump -uroot -p --single-transaction --all-databases > full_backup.sql
  mysqldump -uroot -p mydb > mydb_backup.sql

恢复：
  mysql -uroot -p mydb < mydb_backup.sql
```

#### Day 4-5：binlog
```
学习内容：
  - binlog 是什么（所有写操作的日志）
  - binlog 格式：ROW / STATEMENT / MIXED
  - 用 mysqlbinlog 解析 binlog
  - 基于 binlog 的定点恢复

练习：
  -- 开启 binlog（my.cnf 加 log_bin=mysql-bin）
  -- 做一些数据修改
  -- 删掉一条数据
  -- 用 mysqlbinlog 找到删除前的位置点恢复
```

#### Day 6-7：自动化备份脚本
```
写一个备份脚本（bash 或 Python）：
  - 每日凌晨全量备份
  - 备份保留 7 天
  - 备份文件命名带日期
  - 发送到本地备份目录
  - 验证备份文件可用（导入到测试库检查）
```

### 第 12 周：主从复制 + 连接池

#### Day 1-4：主从复制
```
学习内容：
  - 复制原理（binlog → relay log → SQL 线程重放）
  - 异步复制 vs 半同步复制
  - 主从延迟的原因与监控

Docker 搭建：
  -- 主库
  docker run --name mysql-master -p 3307:3306 -e MYSQL_ROOT_PASSWORD=root \
    -v /path/to/master.cnf:/etc/mysql/conf.d/my.cnf -d mysql:8.0

  -- 从库
  docker run --name mysql-slave -p 3308:3306 -e MYSQL_ROOT_PASSWORD=root \
    -v /path/to/slave.cnf:/etc/mysql/conf.d/my.cnf -d mysql:8.0

  master.cnf:
  [mysqld]
  server_id=1
  log_bin=mysql-bin

  slave.cnf:
  [mysqld]
  server_id=2

练习：
  -- 在主库创建数据库和表
  -- 在从库验证数据同步
  -- 停止从库复制，手动制造延迟
  -- SHOW SLAVE STATUS\G 查看延迟秒数
```

#### Day 5-6：连接池
```
学习内容：
  - 连接池的作用（避免频繁建立 TCP 连接）
  - 常见配置：max_connections, wait_timeout
  - 你的语言驱动连接池配置

练习：
  在你的 API 项目中：
  - 测试无连接池 vs 有连接池的性能差异（压测 100 并发请求）
  - 调优连接池大小（太小阻塞 vs 太大占用资源）
```

#### Day 7：读写分离
```
在 API 代码中实现读写分离：
  - 写操作走主库
  - 读操作走从库
  - 考虑主从延迟（读从库读到旧数据怎么处理）

实现方式选择：
  - 代码层手动路由（简单可控）
  - 中间件（ProxySQL / MySQL Router）
```

**里程碑产出：** Docker 主从复制环境 + 读写分离应用

---

## 第七阶段：数据库生态全貌（穿插进行）

这个阶段不单独占时间，每周花 1-2 小时了解一种非关系型数据库。

### Redis（推荐第 4 周开始穿插）

```
学习内容：
  - 五种基本数据结构（String, Hash, List, Set, ZSet）
  - 缓存模式：Cache Aside
  - 分布式锁（SET NX EX）
  - 常用命令

练习：
  - 搭一个 Redis
  - 在博客 API 里加缓存（文章列表缓存 5 分钟）
  - 用 Redis 做计数器（文章阅读量）
  - 实现分布式锁防止超卖
```

### MongoDB（推荐第 8 周开始穿插）

```
学习内容：
  - 文档模型 vs 关系模型
  - 集合、文档、嵌套文档
  - 基本 CRUD
  - 索引与聚合管道

练习：
  - 装 MongoDB
  - 把博客系统的文章数据存到 MongoDB
  - 对比 MySQL 和 MongoDB 在这个场景的差异
```

### Elasticsearch（推荐第 10 周开始穿插）

```
学习内容：
  - 倒排索引原理
  - 索引、映射、文档
  - 全文搜索 Query DSL
  - Elasticsearch 作为 MySQL 的搜索补充

练习：
  - 搭 ES（Docker）
  - 把博客文章同步到 ES
  - 实现全文搜索接口
```

### 数据库选型决策树

```
需要什么？                        → 选什么？
──────────────────────────────────────────────────────
强一致、复杂关联查询、事务        → MySQL / PostgreSQL
灵活 Schema、快速迭代、JSON 存储  → MongoDB
缓存、计数器、队列、分布式锁      → Redis
全文搜索                          → Elasticsearch
时序数据（监控指标）              → InfluxDB / TimescaleDB
大规模分析查询                    → ClickHouse

现代项目常见组合：
  MySQL + Redis（最经典）
  PostgreSQL + Redis
  MySQL + Elasticsearch + Redis（搜索场景）
```

---

## 每周学习节奏建议

```
工作日（每天 1-1.5 小时）：
  30 分钟 看文档/概念
  30-60 分钟 动手练习

周末（3-4 小时）：
  集中时间完成阶段实战项目

节奏把控：
  - 概念卡住 30 分钟 → 先跳过，回过头再来看
  - 代码跑不通 → 看报错信息，自己解决 >= 30 分钟
  - 每个阶段结束必须产出东西才能进入下一阶段
```

---

## 推荐资源清单

### 书籍
| 书名 | 读哪些章节 | 优先级 |
|------|-----------|--------|
| 《SQL 必知必会》第 5 版 | 全部（快速过） | ★★★ 第 1-2 周 |
| 《高性能 MySQL》第 4 版 | 第 4-6 章（索引、查询优化）<br>第 13-14 章（事务、锁） | ★★★ 第 7-10 周 |
| 《数据库系统概论》第 5 版 | 第 6-7 章（范式、设计理论） | ★★ 第 5 周 |
| 《Redis 设计与实现》 | 第 1-4 章（数据结构） | ★★ 穿插 |
| 《数据密集型应用系统设计》(DDIA) | 第 5-9 章（复制、分区、事务、一致性） | ★★★ 进阶阅读 |

### 在线资源
| 资源 | 用途 | 链接 |
|------|------|------|
| SQLZoo | SQL 交互练习 | sqlzoo.net |
| MySQL 官方文档 | 权威参考 | dev.mysql.com/doc |
| dbdiagram.io | ER 图工具 | dbdiagram.io |
| LeetCode Database | SQL 题练习 | leetcode.com/problemset/database |
| Use The Index, Luke | 索引优化教程 | use-the-index-luke.com |

### 工具清单
```
MySQL 8.0        → 数据库
Docker Desktop   → 环境搭建
DBeaver          → GUI 客户端（免费）
dbdiagram.io     → ER 图
sysbench         → 压测工具
pt-query-digest  → 慢查询分析（Percona Toolkit）
```

---

## 最终目标检验（学完自测）

能独立完成以下所有事项，才算达到中级后端水平：

- [ ] 从零设计一个电商数据库（10 张表以上），画出 ER 图
- [ ] 写出带窗口函数、CTE 的复杂查询
- [ ] 给一个新查询加索引优化，用 EXPLAIN 验证效果
- [ ] 解释 MVCC 原理，说清 REPEATABLE READ 怎么防止幻读
- [ ] 手动制造死锁并分析 SHOW ENGINE INNODB STATUS
- [ ] 用 Docker 搭建主从复制
- [ ] 设计备份策略并写自动备份脚本
- [ ] 说出 MySQL、Redis、MongoDB、ES 各自适合什么场景
- [ ] 处理高并发扣库存（防超卖）
- [ ] 优化一个慢查询并写出优化报告